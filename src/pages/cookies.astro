---
import Badge from "../components/Badge.astro";
import Card from "../components/Card.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Cookies policy"
  description="Cookies policy for PMD marketing and dashboard experiences."
  path="/cookies"
>
  <div class="mx-auto max-w-3xl space-y-8">
    <div class="rounded-3xl border border-white/70 bg-white/70 p-8 text-center shadow-card backdrop-blur">
      <Badge>Cookies</Badge>
      <h1 class="mt-3 text-3xl font-semibold text-ink">Cookies policy</h1>
      <p class="mt-3 text-sm text-slate">
        We use cookies to keep PMD secure, measure performance, and remember your preferences.
      </p>
    </div>

    <Card class="space-y-4">
      <h2 class="text-xl font-semibold text-ink">Cookies we use</h2>
      <p class="text-sm text-slate">
        PMD only uses essential cookies needed to keep the dashboard secure. The marketing site does not set tracking cookies.
      </p>
      <div class="rounded-2xl border border-ink/10 bg-white/80 p-4 text-sm text-slate">
        <div class="grid gap-3 sm:grid-cols-[160px_1fr]">
          <div class="text-xs uppercase tracking-[0.2em] text-ink">Name</div>
          <div class="text-xs uppercase tracking-[0.2em] text-ink">Purpose</div>
          <div class="font-semibold text-ink">pmd_session</div>
          <div>Authentication session cookie for the PMD dashboard (HTTP-only).</div>
          <div class="text-xs uppercase tracking-[0.2em] text-ink">Duration</div>
          <div class="text-xs uppercase tracking-[0.2em] text-ink">Scope</div>
          <div>Up to 14 days (or until you log out).</div>
          <div>Set on the app subdomain only.</div>
        </div>
      </div>
    </Card>

    <Card class="space-y-4">
      <h2 class="text-xl font-semibold text-ink">Analytics cookies</h2>
      <p class="text-sm text-slate">
        We do not use third-party analytics cookies on the marketing site. If this changes, we will update this page.
      </p>
    </Card>

    <Card class="space-y-4">
      <h2 class="text-xl font-semibold text-ink">Managing cookies</h2>
      <p class="text-sm text-slate">
        You can control cookies via your browser settings. Disabling cookies may impact login or dashboard features.
      </p>
    </Card>
  </div>
</BaseLayout>
